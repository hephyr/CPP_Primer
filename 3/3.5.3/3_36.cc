#include <iostream>
#include <vector>
using namespace std;
int main() {
    point
    // int arr1[5], arr2[5];
    // while ()
    // for (int i = 0; i < sizeof(arr1) / sizeof(int); i++)
    //     if (arr1[i] != arr2[i]) {
    //         cout << "Two array is different !" << endl;
    //         break;
    //     }
    // if (i == sizeof(arr1) / sizeof(int))
    //     cout << "Two array is identical ! " << endl;
    vector<int> v1, v2;
    return 0;
}